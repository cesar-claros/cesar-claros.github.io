<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://cesar-claros.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://cesar-claros.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-09-08T19:49:01+00:00</updated><id>https://cesar-claros.github.io/feed.xml</id><title type="html">blank</title><subtitle>Personal academic website of César Claros: research, publications, projects, and CV. </subtitle><entry><title type="html">Working on Caviness with containers and VS Code tunnels</title><link href="https://cesar-claros.github.io/blog/2026/caviness-containers-vscode-tunnels/" rel="alternate" type="text/html" title="Working on Caviness with containers and VS Code tunnels"/><published>2026-09-07T04:30:00+00:00</published><updated>2026-09-07T04:30:00+00:00</updated><id>https://cesar-claros.github.io/blog/2026/caviness-containers-vscode-tunnels</id><content type="html" xml:base="https://cesar-claros.github.io/blog/2026/caviness-containers-vscode-tunnels/"><![CDATA[<p>This post is a guide to my workflow for running experiments on <a href="https://docs.hpc.udel.edu/abstract/caviness/caviness">Caviness</a>, the University of Delaware’s community cluster. For most of my projects I build a container image on my laptop for the environment, keep the code in a GitHub repository, and work on a compute node through a VS Code tunnel, much like I would on my own machine.</p> <p>The post walks through every step from an empty folder to a running tunnel, so someone starting from zero can follow along. Each step ends with the commands, so you can skip the explanations if you only need those.</p> <h2 id="what-you-need">What you need</h2> <ul> <li><strong>A Caviness account and a workgroup.</strong> You need the workgroup name for the <code class="language-plaintext highlighter-rouge">workgroup</code> command below.</li> <li><strong>Docker installed locally.</strong> <a href="https://docs.docker.com/get-docker/">Docker Desktop</a> on macOS or Windows, or Docker Engine on Linux. You build the image here; the cluster only runs it.</li> <li><strong>A Docker Hub account.</strong> A free account with public repositories is enough, since the image only contains libraries.</li> <li><strong>A GitHub account.</strong> VS Code tunnels authenticate with a GitHub or Microsoft account, and both ends must use the same one. I recommend GitHub, because the same account can host your code, and the last section relies on that.</li> <li><strong>VS Code</strong> with the <a href="https://marketplace.visualstudio.com/items?itemName=ms-vscode.remote-server">Remote - Tunnels</a> extension installed.</li> </ul> <p>Everything in this post uses placeholders: <code class="language-plaintext highlighter-rouge">&lt;workgroup&gt;</code> for your Caviness workgroup, <code class="language-plaintext highlighter-rouge">&lt;user&gt;</code> for your cluster username, <code class="language-plaintext highlighter-rouge">&lt;dockerhub-user&gt;</code> for your Docker Hub username, and <code class="language-plaintext highlighter-rouge">&lt;project&gt;</code> for the project name.</p> <h2 id="step-1-separate-the-environment-from-the-code">Step 1: Separate the environment from the code</h2> <p>The container only holds the environment: Python, the libraries, a few system tools, and the VS Code CLI. The code and the data stay on the cluster’s file systems and are bind-mounted into the container at run time. Keeping them apart has two advantages. You only rebuild the image when a dependency changes, which does not happen often, while the code changes every day. And the image can be public, since nothing in it belongs to you.</p> <p>My projects keep the container recipe in its own folder next to the code repository:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;project&gt;/
  code/            git repository, cloned on the cluster too
  container/
    Dockerfile
    pyproject.toml
    start-tunnel.sh
    verify_env.py
</code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">container/</code> folder is the build context. Nothing outside it is visible to <code class="language-plaintext highlighter-rouge">docker build</code>, so the build stays small and the data cannot end up in the image by accident.</p> <h2 id="step-2-declare-the-dependencies">Step 2: Declare the dependencies</h2> <p>The dependencies live in a standard <code class="language-plaintext highlighter-rouge">pyproject.toml</code>, and the image installs them with <a href="https://docs.astral.sh/uv/">uv</a>. Two details are easy to get wrong.</p> <p><strong>The CUDA version has to match the cluster driver.</strong> PyTorch wheels bundle their own CUDA runtime, but the driver on the compute node caps what that runtime can use. On Caviness the driver supports CUDA 12.4, so I pin the <code class="language-plaintext highlighter-rouge">cu124</code> wheel index. If you pick a newer index, <code class="language-plaintext highlighter-rouge">torch.cuda.is_available()</code> returns <code class="language-plaintext highlighter-rouge">False</code> and nothing tells you why.</p> <p><strong>Keep the PyTorch index explicit.</strong> With <code class="language-plaintext highlighter-rouge">explicit = true</code>, uv only consults that index for the packages that name it. Everything else resolves from PyPI as usual.</p> <p>A trimmed version of the file I use:</p> <div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">[</span><span class="n">project</span><span class="k">]</span>
<span class="n">name</span> <span class="o">=</span><span class="w"> </span><span class="s">"&lt;project&gt;"</span>
<span class="n">version</span> <span class="o">=</span><span class="w"> </span><span class="s">"0.1.0"</span>
<span class="n">requires-python</span> <span class="o">=</span><span class="w"> </span><span class="s">"&gt;=3.12"</span>
<span class="n">dependencies</span> <span class="o">=</span><span class="w"> </span><span class="p">[</span>
    <span class="s">"numpy&gt;=2.0.0"</span><span class="p">,</span>
    <span class="s">"pandas&gt;=2.3.0"</span><span class="p">,</span>
    <span class="s">"polars&gt;=1.30.0"</span><span class="p">,</span>
    <span class="s">"scikit-learn&gt;=1.6.0"</span><span class="p">,</span>
    <span class="s">"torch&gt;=2.6.0"</span><span class="p">,</span>
    <span class="s">"torchvision&gt;=0.21.0"</span><span class="p">,</span>
    <span class="s">"lightning&gt;=2.5.0"</span><span class="p">,</span>
    <span class="s">"hydra-core&gt;=1.3.2"</span><span class="p">,</span>
    <span class="s">"wandb&gt;=0.20.0"</span><span class="p">,</span>
    <span class="s">"matplotlib&gt;=3.10.0"</span><span class="p">,</span>
    <span class="s">"ipykernel&gt;=6.29.0"</span><span class="p">,</span>
    <span class="s">"debugpy&gt;=1.8.0"</span><span class="p">,</span>
<span class="p">]</span>

<span class="k">[</span><span class="n">tool</span><span class="k">.</span><span class="n">uv</span><span class="k">.</span><span class="n">sources</span><span class="k">]</span>
<span class="n">torch</span> <span class="o">=</span><span class="w"> </span><span class="p">[{</span><span class="w"> </span><span class="n">index</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"pytorch-cu124"</span><span class="w"> </span><span class="p">}]</span>
<span class="n">torchvision</span> <span class="o">=</span><span class="w"> </span><span class="p">[{</span><span class="w"> </span><span class="n">index</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"pytorch-cu124"</span><span class="w"> </span><span class="p">}]</span>

<span class="k">[[</span><span class="n">tool</span><span class="k">.</span><span class="n">uv</span><span class="k">.</span><span class="n">index</span><span class="k">]]</span>
<span class="n">name</span> <span class="o">=</span><span class="w"> </span><span class="s">"pytorch-cu124"</span>
<span class="n">url</span> <span class="o">=</span><span class="w"> </span><span class="s">"https://download.pytorch.org/whl/cu124"</span>
<span class="n">explicit</span> <span class="o">=</span><span class="w"> </span><span class="kc">true</span>
</code></pre></div></div> <p>VS Code needs <code class="language-plaintext highlighter-rouge">ipykernel</code> to run notebooks inside the container and <code class="language-plaintext highlighter-rouge">debugpy</code> for the Python debugger.</p> <h2 id="step-3-write-the-dockerfile">Step 3: Write the Dockerfile</h2> <p>The Dockerfile has two stages. The builder stage installs the dependencies with uv. The runtime stage starts from the same slim base, copies the installed packages over, adds the handful of system tools the slim image lacks, and installs the VS Code CLI. uv and the build tools stay in the first stage.</p> <div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># syntax=docker/dockerfile:1</span>
<span class="c">#</span>
<span class="k">ARG</span><span class="s"> PYTHON_VERSION=3.12</span>

<span class="c"># -----------------------</span>
<span class="c"># Stage 1: builder</span>
<span class="c"># -----------------------</span>
<span class="k">FROM</span><span class="w"> </span><span class="s">python:${PYTHON_VERSION}-slim</span><span class="w"> </span><span class="k">AS</span><span class="w"> </span><span class="s">builder</span>

<span class="k">ENV</span><span class="s"> DEBIAN_FRONTEND=noninteractive</span>
<span class="k">WORKDIR</span><span class="s"> /project</span>

<span class="c"># uv, copied from its official image</span>
<span class="k">COPY</span><span class="s"> --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv</span>

<span class="k">ENV</span><span class="s"> UV_LINK_MODE=copy \</span>
    UV_COMPILE_BYTECODE=1 \
    UV_PYTHON_DOWNLOADS=never

<span class="c"># Only the dependency file enters the context, so this layer is cached</span>
<span class="c"># until pyproject.toml changes.</span>
<span class="k">COPY</span><span class="s"> pyproject.toml ./</span>

<span class="c"># Install into the image's system Python, then drop uv.</span>
<span class="k">RUN </span>uv pip <span class="nb">install</span> <span class="se">\
</span>      <span class="nt">--python</span> /usr/local/bin/python3 <span class="se">\
</span>      <span class="nt">--system</span> <span class="nb">.</span> <span class="se">\
</span>      <span class="nt">--no-cache-dir</span> <span class="se">\
</span> <span class="o">&amp;&amp;</span> <span class="nb">rm</span> <span class="nt">-f</span> /usr/local/bin/uv

<span class="c"># -----------------------</span>
<span class="c"># Stage 2: runtime</span>
<span class="c"># -----------------------</span>
<span class="k">FROM</span><span class="w"> </span><span class="s">python:${PYTHON_VERSION}-slim</span><span class="w"> </span><span class="k">AS</span><span class="w"> </span><span class="s">runtime</span>

<span class="k">ENV</span><span class="s"> DEBIAN_FRONTEND=noninteractive</span>
<span class="k">WORKDIR</span><span class="s"> /project</span>

<span class="c"># ca-certificates and curl: download the VS Code CLI and let it reach GitHub.</span>
<span class="c"># git: pull and push from inside the tunnel.</span>
<span class="c"># tini: a small init as PID 1, so signals reach `code tunnel` cleanly.</span>
<span class="c"># libgomp1: OpenMP runtime that xgboost, catboost, and scikit-learn wheels link against.</span>
<span class="k">RUN </span>apt-get update <span class="o">&amp;&amp;</span> apt-get <span class="nb">install</span> <span class="nt">-y</span> <span class="nt">--no-install-recommends</span> <span class="se">\
</span>      ca-certificates curl git tini libgomp1 <span class="se">\
</span>    <span class="o">&amp;&amp;</span> <span class="nb">rm</span> <span class="nt">-rf</span> /var/lib/apt/lists/<span class="k">*</span>

<span class="c"># Same base image in both stages, so /usr/local can be copied as a whole.</span>
<span class="k">COPY</span><span class="s"> --from=builder /usr/local /usr/local</span>

<span class="k">ENV</span><span class="s"> PATH="/usr/local/bin:$PATH" \</span>
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1

<span class="c"># VS Code CLI. The alpine build is statically linked, so it runs on any</span>
<span class="c"># x86_64 Linux, including inside Singularity on the cluster.</span>
<span class="k">RUN </span><span class="nb">set</span> <span class="nt">-eux</span><span class="p">;</span> <span class="se">\
</span>    <span class="nb">mkdir</span> <span class="nt">-p</span> /tmp/vscode-cli<span class="p">;</span> <span class="se">\
</span>    curl <span class="nt">-L</span> <span class="s1">'https://code.visualstudio.com/sha/download?build=stable&amp;os=cli-alpine-x64'</span> <span class="se">\
</span>      <span class="nt">-o</span> /tmp/vscode-cli.tar.gz<span class="p">;</span> <span class="se">\
</span>    <span class="nb">tar</span> <span class="nt">-xzf</span> /tmp/vscode-cli.tar.gz <span class="nt">-C</span> /tmp/vscode-cli<span class="p">;</span> <span class="se">\
</span>    <span class="nb">install</span> <span class="nt">-m</span> 0755 /tmp/vscode-cli/code /usr/local/bin/code<span class="p">;</span> <span class="se">\
</span>    <span class="nb">rm</span> <span class="nt">-rf</span> /tmp/vscode-cli /tmp/vscode-cli.tar.gz

<span class="k">COPY</span><span class="s"> start-tunnel.sh /usr/local/bin/start-tunnel.sh</span>
<span class="k">RUN </span><span class="nb">chmod</span> +x /usr/local/bin/start-tunnel.sh

<span class="k">ENTRYPOINT</span><span class="s"> ["/usr/bin/tini", "-s", "--"]</span>
<span class="k">CMD</span><span class="s"> ["/usr/local/bin/start-tunnel.sh"]</span>
</code></pre></div></div> <p>A few decisions in this file are not obvious.</p> <p><strong>No virtual environment.</strong> Installing into the system Python of the official image means there is nothing to activate. <code class="language-plaintext highlighter-rouge">python</code> inside the container is always the right interpreter, whether you run it through the tunnel, through <code class="language-plaintext highlighter-rouge">singularity exec</code>, or in a batch job.</p> <p><strong>Why <code class="language-plaintext highlighter-rouge">tini</code>.</strong> Singularity runs the container’s command as a normal process, and <code class="language-plaintext highlighter-rouge">code tunnel</code> expects to receive signals directly. <code class="language-plaintext highlighter-rouge">tini</code> forwards them and reaps child processes, so the tunnel shuts down cleanly when Slurm cancels the job.</p> <p><strong>No CUDA toolkit.</strong> The PyTorch wheels ship their own CUDA runtime, and Singularity’s <code class="language-plaintext highlighter-rouge">--nv</code> flag brings in the host driver. Without the toolkit the image is around two gigabytes instead of eight.</p> <h2 id="step-4-the-tunnel-entry-script">Step 4: The tunnel entry script</h2> <p>The default command of the image starts a VS Code tunnel. Two environment variables adapt it to a shared cluster.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/sh</span>
<span class="c"># Start a VS Code tunnel named after this node, with per-node CLI state.</span>
<span class="c">#</span>
<span class="c"># TUNNEL_NAME: tunnel name shown in VS Code. Defaults to the node hostname.</span>
<span class="c">#   Rules: lowercase letters, digits, and hyphens, 4 to 20 characters.</span>
<span class="c"># TUNNEL_DATA_DIR: where the CLI keeps its state, the singleton lock, and the</span>
<span class="c">#   downloaded server. Defaults to a per-node directory under $HOME.</span>
<span class="nb">set</span> <span class="nt">-eu</span>

<span class="nv">name</span><span class="o">=</span><span class="s2">"</span><span class="k">${</span><span class="nv">TUNNEL_NAME</span><span class="k">:-</span><span class="si">$(</span><span class="nb">uname</span> <span class="nt">-n</span> | <span class="nb">cut</span> <span class="nt">-d</span><span class="nb">.</span> <span class="nt">-f1</span> | <span class="nb">tr</span> <span class="s1">'A-Z'</span> <span class="s1">'a-z'</span> | <span class="nb">tr</span> <span class="nt">-cs</span> <span class="s1">'a-z0-9'</span> <span class="s1">'-'</span> | <span class="nb">sed</span> <span class="s1">'s/^-//;s/-$//'</span> | <span class="nb">cut</span> <span class="nt">-c1-20</span><span class="si">)</span><span class="k">}</span><span class="s2">"</span>
<span class="nv">data_dir</span><span class="o">=</span><span class="s2">"</span><span class="k">${</span><span class="nv">TUNNEL_DATA_DIR</span><span class="k">:-${</span><span class="nv">HOME</span><span class="k">:-</span><span class="p">/tmp</span><span class="k">}</span><span class="p">/.vscode-cli-</span><span class="k">${</span><span class="nv">name</span><span class="k">}}</span><span class="s2">"</span>
<span class="nb">mkdir</span> <span class="nt">-p</span> <span class="s2">"</span><span class="nv">$data_dir</span><span class="s2">"</span>

<span class="c"># exec so `code tunnel` replaces this shell and becomes tini's direct child.</span>
<span class="nb">exec </span>code tunnel <span class="nt">--accept-server-license-terms</span> <span class="nt">--name</span> <span class="s2">"</span><span class="nv">$name</span><span class="s2">"</span> <span class="nt">--cli-data-dir</span> <span class="s2">"</span><span class="nv">$data_dir</span><span class="s2">"</span> <span class="s2">"</span><span class="nv">$@</span><span class="s2">"</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">TUNNEL_NAME</code> is the name that shows up in the VS Code Remote Explorer. The default is the node’s hostname, which is convenient on a cluster because it tells you where you are running.</p> <p><code class="language-plaintext highlighter-rouge">TUNNEL_DATA_DIR</code> fixes a problem you will hit the second time you start a tunnel. The CLI keeps a lock file in its data directory. Your home directory is the same on every node, so two tunnels on two nodes fight over one lock, and the second one dies with <code class="language-plaintext highlighter-rouge">error access singleton</code>. Giving each tunnel its own directory, keyed by node name, avoids that. If your home quota is tight, point it at node-local scratch instead, as shown later.</p> <h2 id="step-5-build-and-test-the-image-locally">Step 5: Build and test the image locally</h2> <p>Caviness nodes are x86_64. If your laptop is an Apple Silicon Mac, Docker builds ARM images by default, and the cluster will refuse them with <code class="language-plaintext highlighter-rouge">exec format error</code>. Always pass the platform flag.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> &lt;project&gt;
docker build <span class="nt">--platform</span> linux/amd64 <span class="nt">-t</span> &lt;dockerhub-user&gt;/&lt;project&gt;:v1 container/
</code></pre></div></div> <p>The first build downloads PyTorch and takes a while. Later builds reuse the cached layers unless <code class="language-plaintext highlighter-rouge">pyproject.toml</code> changed.</p> <p>Test the image before pushing it. The tunnel needs no GPU to start, and the imports need no GPU to succeed:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">--rm</span> &lt;dockerhub-user&gt;/&lt;project&gt;:v1 python <span class="nt">-c</span> <span class="s2">"import torch, lightning; print(torch.__version__)"</span>
docker run <span class="nt">--rm</span> <span class="nt">-it</span> <span class="nt">--entrypoint</span> bash &lt;dockerhub-user&gt;/&lt;project&gt;:v1
</code></pre></div></div> <p>The second command drops you into a shell inside the image, which is the fastest way to check that a tool you expect is there.</p> <blockquote class="block-tip"> <p>On an Apple Silicon Mac the <code class="language-plaintext highlighter-rouge">linux/amd64</code> image runs under emulation, so it is slow. That is fine for checking imports, but do not time anything there.</p> </blockquote> <h2 id="step-6-push-the-image-to-docker-hub">Step 6: Push the image to Docker Hub</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker login
docker push &lt;dockerhub-user&gt;/&lt;project&gt;:v1
</code></pre></div></div> <p>Use a version tag instead of <code class="language-plaintext highlighter-rouge">latest</code>. When you rebuild with new dependencies, push <code class="language-plaintext highlighter-rouge">v2</code>. The old <code class="language-plaintext highlighter-rouge">.sif</code> on the cluster keeps working until you decide to switch, and you always know which environment produced which result.</p> <h2 id="step-7-pull-the-image-on-caviness">Step 7: Pull the image on Caviness</h2> <p>Log in, set your workgroup, and load Singularity.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh &lt;user&gt;@caviness.hpc.udel.edu
workgroup <span class="nt">-g</span> &lt;workgroup&gt;
vpkg_require singularity
</code></pre></div></div> <p>Before pulling, decide where things live. Caviness has <a href="https://docs.hpc.udel.edu/abstract/caviness/filesystems/filesystems">four file systems</a>, and each has a different purpose.</p> <table> <thead> <tr> <th>Location</th> <th>What goes there</th> <th>Why</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">/home/&lt;uid&gt;</code></td> <td>Dotfiles, SSH keys</td> <td>20 GB quota. An image pull alone can fill it.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">/work/&lt;workgroup&gt;</code></td> <td>The <code class="language-plaintext highlighter-rouge">.sif</code>, the code repository, the Singularity cache</td> <td>Shared group storage, at least 1 TB, backed up. Executables are allowed here.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">/lustre/scratch</code></td> <td>Datasets and large intermediate files</td> <td>Fast parallel file system, but purged periodically and no executables allowed.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">$TMPDIR</code></td> <td>Per-job scratch, the tunnel’s CLI state</td> <td>Node-local disk, gone when the job ends.</td> </tr> </tbody> </table> <p>One setting to get right on the first day is the cache directory. Singularity unpacks every Docker layer into a cache before assembling the <code class="language-plaintext highlighter-rouge">.sif</code>, and by default that cache is in your home directory. Point it at your workgroup storage, and put the export in your <code class="language-plaintext highlighter-rouge">.bashrc</code> so you do not have to remember it.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> /work/&lt;workgroup&gt;/&lt;user&gt;/containers /work/&lt;workgroup&gt;/&lt;user&gt;/.singularity
<span class="nb">export </span><span class="nv">SINGULARITY_CACHEDIR</span><span class="o">=</span>/work/&lt;workgroup&gt;/&lt;user&gt;/.singularity

<span class="nb">cd</span> /work/&lt;workgroup&gt;/&lt;user&gt;/containers
singularity pull &lt;project&gt;.sif docker://&lt;dockerhub-user&gt;/&lt;project&gt;:v1
</code></pre></div></div> <p>The pull runs on the login node, which has internet access, and produces a single file, <code class="language-plaintext highlighter-rouge">&lt;project&gt;.sif</code>. Everything after this point runs on a compute node.</p> <h2 id="step-8-get-a-compute-node">Step 8: Get a compute node</h2> <p>Interactive work goes through <code class="language-plaintext highlighter-rouge">salloc</code>. The <code class="language-plaintext highlighter-rouge">_workgroup_</code> partition resolves to your own workgroup’s priority nodes; use <code class="language-plaintext highlighter-rouge">standard</code> if you are fine with preemption, or <code class="language-plaintext highlighter-rouge">devel</code> for quick tests. Request only what you need, since a GPU sitting idle in an interactive session is unavailable to everyone else.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>salloc <span class="nt">--partition</span><span class="o">=</span>_workgroup_ <span class="nt">--gres</span><span class="o">=</span>gpu:1 <span class="nt">--cpus-per-task</span><span class="o">=</span>8 <span class="nt">--mem</span><span class="o">=</span>48G <span class="nt">--time</span><span class="o">=</span>04:00:00
</code></pre></div></div> <p>To ask for a specific GPU type, replace <code class="language-plaintext highlighter-rouge">--gres=gpu:1</code> with <code class="language-plaintext highlighter-rouge">--gres=gpu:v100:1</code> (Caviness has <code class="language-plaintext highlighter-rouge">p100</code>, <code class="language-plaintext highlighter-rouge">v100</code>, <code class="language-plaintext highlighter-rouge">t4</code>, and <code class="language-plaintext highlighter-rouge">a100</code> nodes, depending on the workgroup). When the allocation starts, your prompt changes to the compute node’s hostname. Everything from here on runs on that node.</p> <blockquote class="block-tip"> <p>An interactive allocation ends when your SSH session ends. If your connection is unreliable, start <code class="language-plaintext highlighter-rouge">tmux</code> on the login node before running <code class="language-plaintext highlighter-rouge">salloc</code>, so a dropped connection does not kill the job.</p> </blockquote> <h2 id="step-9-verify-the-environment">Step 9: Verify the environment</h2> <p>Before opening an editor, confirm that the image sees the GPU. This small script checks the pieces that fail most often, in dependency order, so the first failure points at the cause:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">#!/usr/bin/env python
</span><span class="sh">"""</span><span class="s">Sanity checks for the container. Run: singularity exec --nv &lt;project&gt;.sif python verify_env.py</span><span class="sh">"""</span>
<span class="kn">import</span> <span class="n">importlib</span>
<span class="kn">import</span> <span class="n">sys</span>

<span class="n">failures</span> <span class="o">=</span> <span class="mi">0</span>


<span class="k">def</span> <span class="nf">check</span><span class="p">(</span><span class="n">label</span><span class="p">,</span> <span class="n">fn</span><span class="p">):</span>
    <span class="k">global</span> <span class="n">failures</span>
    <span class="k">try</span><span class="p">:</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">[ OK ] </span><span class="si">{</span><span class="n">label</span><span class="si">}</span><span class="s">: </span><span class="si">{</span><span class="nf">fn</span><span class="p">()</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">exc</span><span class="p">:</span>  <span class="c1"># noqa: BLE001
</span>        <span class="n">failures</span> <span class="o">+=</span> <span class="mi">1</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">[FAIL] </span><span class="si">{</span><span class="n">label</span><span class="si">}</span><span class="s">: </span><span class="si">{</span><span class="nf">type</span><span class="p">(</span><span class="n">exc</span><span class="p">).</span><span class="n">__name__</span><span class="si">}</span><span class="s">: </span><span class="si">{</span><span class="n">exc</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>


<span class="nf">check</span><span class="p">(</span><span class="sh">"</span><span class="s">python</span><span class="sh">"</span><span class="p">,</span> <span class="k">lambda</span><span class="p">:</span> <span class="n">sys</span><span class="p">.</span><span class="n">version</span><span class="p">.</span><span class="nf">split</span><span class="p">()[</span><span class="mi">0</span><span class="p">])</span>
<span class="nf">check</span><span class="p">(</span><span class="sh">"</span><span class="s">torch</span><span class="sh">"</span><span class="p">,</span> <span class="k">lambda</span><span class="p">:</span> <span class="n">importlib</span><span class="p">.</span><span class="nf">import_module</span><span class="p">(</span><span class="sh">"</span><span class="s">torch</span><span class="sh">"</span><span class="p">).</span><span class="n">__version__</span><span class="p">)</span>
<span class="nf">check</span><span class="p">(</span><span class="sh">"</span><span class="s">torch cuda build</span><span class="sh">"</span><span class="p">,</span> <span class="k">lambda</span><span class="p">:</span> <span class="n">importlib</span><span class="p">.</span><span class="nf">import_module</span><span class="p">(</span><span class="sh">"</span><span class="s">torch</span><span class="sh">"</span><span class="p">).</span><span class="n">version</span><span class="p">.</span><span class="n">cuda</span><span class="p">)</span>
<span class="nf">check</span><span class="p">(</span><span class="sh">"</span><span class="s">cuda available</span><span class="sh">"</span><span class="p">,</span> <span class="k">lambda</span><span class="p">:</span> <span class="n">importlib</span><span class="p">.</span><span class="nf">import_module</span><span class="p">(</span><span class="sh">"</span><span class="s">torch</span><span class="sh">"</span><span class="p">).</span><span class="n">cuda</span><span class="p">.</span><span class="nf">is_available</span><span class="p">()</span> <span class="ow">or</span> <span class="p">(</span><span class="n">_</span> <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="p">()).</span><span class="nf">throw</span><span class="p">(</span><span class="nc">RuntimeError</span><span class="p">(</span><span class="sh">"</span><span class="s">no GPU</span><span class="sh">"</span><span class="p">)))</span>
<span class="nf">check</span><span class="p">(</span><span class="sh">"</span><span class="s">gpu</span><span class="sh">"</span><span class="p">,</span> <span class="k">lambda</span><span class="p">:</span> <span class="n">importlib</span><span class="p">.</span><span class="nf">import_module</span><span class="p">(</span><span class="sh">"</span><span class="s">torch</span><span class="sh">"</span><span class="p">).</span><span class="n">cuda</span><span class="p">.</span><span class="nf">get_device_name</span><span class="p">(</span><span class="mi">0</span><span class="p">))</span>
<span class="k">for</span> <span class="n">pkg</span> <span class="ow">in</span> <span class="p">[</span><span class="sh">"</span><span class="s">numpy</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">pandas</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">polars</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">sklearn</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">lightning</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">hydra</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">wandb</span><span class="sh">"</span><span class="p">]:</span>
    <span class="nf">check</span><span class="p">(</span><span class="n">pkg</span><span class="p">,</span> <span class="k">lambda</span> <span class="n">p</span><span class="o">=</span><span class="n">pkg</span><span class="p">:</span> <span class="n">importlib</span><span class="p">.</span><span class="nf">import_module</span><span class="p">(</span><span class="n">p</span><span class="p">).</span><span class="n">__version__</span><span class="p">)</span>

<span class="n">sys</span><span class="p">.</span><span class="nf">exit</span><span class="p">(</span><span class="mi">1</span> <span class="k">if</span> <span class="n">failures</span> <span class="k">else</span> <span class="mi">0</span><span class="p">)</span>
</code></pre></div></div> <p>Keep it in <code class="language-plaintext highlighter-rouge">container/</code> and run it through the image:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /work/&lt;workgroup&gt;/&lt;user&gt;/&lt;project&gt;
singularity <span class="nb">exec</span> <span class="nt">--nv</span> containers/&lt;project&gt;.sif python container/verify_env.py
</code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">--nv</code> flag mounts the host’s NVIDIA driver into the container. Without it, the <code class="language-plaintext highlighter-rouge">cuda available</code> check fails and everything else passes.</p> <h2 id="step-10-start-the-tunnel-and-connect">Step 10: Start the tunnel and connect</h2> <p>Run the image. Its default command is the tunnel script from Step 4.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>singularity run <span class="nt">--nv</span> <span class="se">\</span>
  <span class="nt">--env</span> <span class="nv">TUNNEL_NAME</span><span class="o">=</span>&lt;project&gt;-gpu <span class="se">\</span>
  <span class="nt">--env</span> <span class="nv">TUNNEL_DATA_DIR</span><span class="o">=</span><span class="nv">$TMPDIR</span>/vscode-cli <span class="se">\</span>
  <span class="nt">-B</span> /work/&lt;workgroup&gt;:/work/&lt;workgroup&gt; <span class="se">\</span>
  /work/&lt;workgroup&gt;/&lt;user&gt;/containers/&lt;project&gt;.sif
</code></pre></div></div> <p>The bind flag makes your workgroup storage visible inside the container at the same path. Singularity binds your home directory and <code class="language-plaintext highlighter-rouge">/tmp</code> on its own; anything else you want to see from the editor needs a <code class="language-plaintext highlighter-rouge">-B</code>.</p> <p>The first time, the CLI asks how to sign in. Choose GitHub, open the printed <code class="language-plaintext highlighter-rouge">https://github.com/login/device</code> URL in your laptop’s browser, and enter the one-time code. This links the tunnel to your GitHub account, and only that account can connect to it. On later runs the CLI reuses the stored credentials, as long as <code class="language-plaintext highlighter-rouge">TUNNEL_DATA_DIR</code> still exists. Because node-local scratch is wiped at the end of the job, you will re-authenticate when you use <code class="language-plaintext highlighter-rouge">$TMPDIR</code>; if you would rather not, keep the data directory under <code class="language-plaintext highlighter-rouge">/work/&lt;workgroup&gt;/&lt;user&gt;/.vscode-cli-&lt;node&gt;</code> instead.</p> <p>Now on your laptop: open VS Code, open the Remote Explorer view, pick <strong>Tunnels</strong> in the dropdown, and connect to <code class="language-plaintext highlighter-rouge">&lt;project&gt;-gpu</code>. VS Code installs its server through the tunnel on first use, then opens a window whose terminal, Python interpreter, debugger, and notebooks all run inside the container on the compute node. Open the folder <code class="language-plaintext highlighter-rouge">/work/&lt;workgroup&gt;/&lt;user&gt;/&lt;project&gt;/code</code> and you are working on the cluster.</p> <p>The tunnel stays up as long as the <code class="language-plaintext highlighter-rouge">singularity run</code> process, which ends with the allocation. When the time limit is reached the window disconnects, and to get back in you request a new node and start the tunnel again.</p> <h2 id="step-11-keep-the-code-in-github">Step 11: Keep the code in GitHub</h2> <p>The tunnel lets you edit directly on the cluster, and for quick fixes I do. For anything larger I edit on my laptop, push, and pull on the cluster. Local tools are faster and always available, including AI coding assistants such as Claude Code or Copilot, which work on a local checkout. And since every change passes through GitHub, the git history tells you which code ran when, which helps a lot when writing up results.</p> <p>One-time setup on the cluster:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /work/&lt;workgroup&gt;/&lt;user&gt;/&lt;project&gt;
git clone git@github.com:&lt;github-user&gt;/&lt;project&gt;.git code
</code></pre></div></div> <p>Add your cluster SSH key to your GitHub account so that pushes and pulls do not prompt for a password. Then the daily loop is:</p> <ol> <li>Edit on the laptop. Run the fast checks that need no GPU: lint, type check, unit tests.</li> <li>Commit and push.</li> <li>In the tunnel terminal on the cluster, <code class="language-plaintext highlighter-rouge">git pull</code>, then run the experiment.</li> <li>Results and logs land in <code class="language-plaintext highlighter-rouge">/work</code> or <code class="language-plaintext highlighter-rouge">/lustre</code>. Pull the small ones back with <code class="language-plaintext highlighter-rouge">rsync</code>; commit only figures and tables, never checkpoints.</li> </ol> <p>Compute nodes have limited internet access. In my experience the tunnel connects fine from a compute node, but large downloads such as model weights, pretrained checkpoints, and datasets should happen on the login node, which has full access. If <code class="language-plaintext highlighter-rouge">git pull</code> stalls on a compute node, run it from a login-node shell instead. The container includes <code class="language-plaintext highlighter-rouge">git</code>, so the pull works from either the tunnel terminal or the host shell.</p> <h2 id="step-12-batch-jobs-without-a-tunnel">Step 12: Batch jobs without a tunnel</h2> <p>The tunnel is for development. Once a script runs end to end, submit it as a batch job so it does not depend on your laptop being awake. The same image runs the script through <code class="language-plaintext highlighter-rouge">singularity exec</code>, with the same bind mounts.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c">#SBATCH --job-name=&lt;project&gt;-train</span>
<span class="c">#SBATCH --partition=_workgroup_</span>
<span class="c">#SBATCH --gres=gpu:1</span>
<span class="c">#SBATCH --cpus-per-task=8</span>
<span class="c">#SBATCH --mem=48G</span>
<span class="c">#SBATCH --time=12:00:00</span>
<span class="c">#SBATCH --output=/work/&lt;workgroup&gt;/&lt;user&gt;/&lt;project&gt;/logs/%x-%j.out</span>

vpkg_require singularity
<span class="nb">export </span><span class="nv">SINGULARITY_CACHEDIR</span><span class="o">=</span>/work/&lt;workgroup&gt;/&lt;user&gt;/.singularity

<span class="nv">SIF</span><span class="o">=</span>/work/&lt;workgroup&gt;/&lt;user&gt;/containers/&lt;project&gt;.sif
<span class="nv">CODE</span><span class="o">=</span>/work/&lt;workgroup&gt;/&lt;user&gt;/&lt;project&gt;/code

<span class="nb">cd</span> <span class="s2">"</span><span class="nv">$CODE</span><span class="s2">"</span>
singularity <span class="nb">exec</span> <span class="nt">--nv</span> <span class="se">\</span>
  <span class="nt">-B</span> /work/&lt;workgroup&gt;:/work/&lt;workgroup&gt; <span class="se">\</span>
  <span class="nt">-B</span> /lustre/scratch/&lt;user&gt;:/lustre/scratch/&lt;user&gt; <span class="se">\</span>
  <span class="s2">"</span><span class="nv">$SIF</span><span class="s2">"</span> python train.py <span class="s2">"</span><span class="nv">$@</span><span class="s2">"</span>
</code></pre></div></div> <p>Save it as <code class="language-plaintext highlighter-rouge">train.qs</code>, then:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>workgroup <span class="nt">-g</span> &lt;workgroup&gt;
<span class="nb">mkdir</span> <span class="nt">-p</span> /work/&lt;workgroup&gt;/&lt;user&gt;/&lt;project&gt;/logs
sbatch train.qs
squeue <span class="nt">-u</span> &lt;user&gt;
<span class="nb">tail</span> <span class="nt">-f</span> /work/&lt;workgroup&gt;/&lt;user&gt;/&lt;project&gt;/logs/&lt;project&gt;-train-&lt;jobid&gt;.out
</code></pre></div></div> <p>Anything after <code class="language-plaintext highlighter-rouge">sbatch train.qs</code> on the command line is forwarded to the script through <code class="language-plaintext highlighter-rouge">"$@"</code>, so Hydra overrides and flags pass straight through.</p> <p>If you want to watch a long run without a batch job, launch it in the background inside the interactive allocation and keep the tunnel open next to it.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">nohup </span>singularity <span class="nb">exec</span> <span class="nt">--nv</span> <span class="nt">-B</span> /work/&lt;workgroup&gt;:/work/&lt;workgroup&gt; <span class="se">\</span>
  /work/&lt;workgroup&gt;/&lt;user&gt;/containers/&lt;project&gt;.sif python train.py <span class="o">&gt;</span> train.log 2&gt;&amp;1 &amp;
<span class="nb">tail</span> <span class="nt">-f</span> train.log
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">nohup</code> survives a dropped SSH session but not the end of the allocation, so request enough time.</p> <h2 id="step-13-update-the-environment">Step 13: Update the environment</h2> <p>When a dependency changes, the loop is: edit <code class="language-plaintext highlighter-rouge">pyproject.toml</code>, rebuild, push with a new tag, pull the new tag on the cluster.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># laptop</span>
docker build <span class="nt">--platform</span> linux/amd64 <span class="nt">-t</span> &lt;dockerhub-user&gt;/&lt;project&gt;:v2 container/
docker push &lt;dockerhub-user&gt;/&lt;project&gt;:v2

<span class="c"># caviness login node</span>
<span class="nb">cd</span> /work/&lt;workgroup&gt;/&lt;user&gt;/containers
singularity pull &lt;project&gt;-v2.sif docker://&lt;dockerhub-user&gt;/&lt;project&gt;:v2
</code></pre></div></div> <p>Keep the old <code class="language-plaintext highlighter-rouge">.sif</code> until you have re-run the verification script and a smoke test with the new one. Then update the path in your job scripts. If you want to pin exact versions after a successful build, freeze them from inside the image and commit the result next to <code class="language-plaintext highlighter-rouge">pyproject.toml</code>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>singularity <span class="nb">exec</span> &lt;project&gt;-v2.sif python <span class="nt">-m</span> pip freeze <span class="o">&gt;</span> container/requirements.lock
</code></pre></div></div> <h2 id="troubleshooting">Troubleshooting</h2> <ul> <li><strong><code class="language-plaintext highlighter-rouge">exec format error</code> on the cluster.</strong> The image was built for ARM. Rebuild with <code class="language-plaintext highlighter-rouge">--platform linux/amd64</code>.</li> <li><strong><code class="language-plaintext highlighter-rouge">error access singleton</code> when starting the tunnel.</strong> Another tunnel, possibly on another node, holds the lock in the same data directory. Set <code class="language-plaintext highlighter-rouge">TUNNEL_DATA_DIR</code> per node, as in Step 4.</li> <li><strong>Disk quota exceeded during <code class="language-plaintext highlighter-rouge">singularity pull</code>.</strong> The layer cache went to your home directory. Set <code class="language-plaintext highlighter-rouge">SINGULARITY_CACHEDIR</code> to <code class="language-plaintext highlighter-rouge">/work</code> and pull again.</li> <li><strong><code class="language-plaintext highlighter-rouge">cuda available: RuntimeError: no GPU</code>.</strong> Either you forgot <code class="language-plaintext highlighter-rouge">--nv</code>, or you are on the login node, or the wheels target a CUDA version newer than the driver supports. Check the <code class="language-plaintext highlighter-rouge">torch cuda build</code> line against 12.4.</li> <li><strong>Tunnel name rejected.</strong> Names are 4 to 20 characters of lowercase letters, digits, and hyphens.</li> <li><strong>Locale warnings from Python or git.</strong> Start the container with <code class="language-plaintext highlighter-rouge">LANG=C.UTF-8</code> in front of the <code class="language-plaintext highlighter-rouge">singularity</code> command.</li> <li><strong>VS Code shows the tunnel but cannot connect.</strong> The allocation ended. Check <code class="language-plaintext highlighter-rouge">squeue -u &lt;user&gt;</code>; if the job is gone, request a new node and start the tunnel again.</li> </ul> <h2 id="closing">Closing</h2> <p>The whole setup comes down to three files in a <code class="language-plaintext highlighter-rouge">container/</code> folder and about ten commands. In return, the environment is the same on every node and every rerun, the editor runs next to the GPU, and every change goes through GitHub whether I type it on the laptop or on the cluster. The recipe in this post is the one I use in my current projects, minus the project-specific dependencies. Copy it, edit the <code class="language-plaintext highlighter-rouge">pyproject.toml</code>, and it should work as is.</p>]]></content><author><name></name></author><category term="tooling"/><category term="hpc"/><category term="docker"/><category term="singularity"/><category term="vscode"/><category term="caviness"/><summary type="html"><![CDATA[A step-by-step setup for a reproducible Python environment on the University of Delaware's Caviness cluster. Build a Docker image on your laptop, pull it as a Singularity image on the cluster, open a VS Code tunnel on a GPU node, and keep the code in GitHub.]]></summary></entry></feed>